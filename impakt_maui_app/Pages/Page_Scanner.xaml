<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="impakt_maui_app.Pages.Page_Scanner"
             xmlns:vm="clr-namespace:impakt_maui_app.VM"
             x:Name="this"
             Background="{StaticResource PrimeWhite}"
             Title="QR Scanner">

    <ContentPage.BindingContext>
        <vm:VM_Scanner />
    </ContentPage.BindingContext>

    <ScrollView>
        <Grid
            RowDefinitions="Auto,Auto,Auto"
            ColumnDefinitions="*,*"
            RowSpacing="8" ColumnSpacing="8"
            Margin="6" Padding="5">

            <!--Scan-->
            <Border>
                
            </Border>

            <!--Buy pass-->
            <Border>

            </Border>

            <!--Member info-->
            <Border>

            </Border>
        </Grid>
        <!--<VerticalStackLayout Padding="10" Spacing="10">
            <Button x:Name="Btn_CheckIn" Text="CheckIn" 
                    CornerRadius="10" BackgroundColor="#007AFF" TextColor="White"
                    Clicked="OnClicked_CheckIn"/>
            
            <Button x:Name="Btn_UpdatePass" Text="Update pass" 
                    CornerRadius="10" BackgroundColor="#007AFF" TextColor="White"
                    Clicked="OnClicked_UpdatePass"/>
            
            <Button x:Name="Btn_UserInfo" Text="User info" 
                    CornerRadius="10" BackgroundColor="#007AFF" TextColor="White"
                    Clicked="OnClicked_UserInfo"/>
        </VerticalStackLayout>-->
    </ScrollView>
</ContentPage>