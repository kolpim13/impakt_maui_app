<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
			   xmlns:controls="clr-namespace:impakt_maui_app.Reusable"
			   xmlns:vm="clr-namespace:impakt_maui_app.VM"
               x:Class="impakt_maui_app.Popups.Popup_MemberInfo"
			   x:Name="Popup_MemberInfo_Inst">

	<toolkit:Popup.Resources>
		<!-- 1) Define two styles -->
		<Style x:Key="StaticLabelStyle" TargetType="Label">
			<Setter Property="FontAttributes" Value="Bold" />
			<Setter Property="TextColor" Value="Gray" />
			<Setter Property="FontSize" Value="Medium" />
			<Setter Property="VerticalTextAlignment" Value="Center" />
			<Setter Property="HorizontalTextAlignment" Value="End" />
		</Style>

		<Style x:Key="DynamicLabelStyle" TargetType="Label">
			<Setter Property="FontAttributes" Value="None" />
			<Setter Property="TextColor" Value="Black" />
			<Setter Property="FontSize" Value="Medium" />
			<Setter Property="VerticalTextAlignment" Value="Center" />
			<Setter Property="HorizontalTextAlignment" Value="Start" />
			<!-- subtle background for emphasis -->
			<Setter Property="BackgroundColor" Value="#F5F5F5" />
			<Setter Property="Padding" Value="4,2" />
		</Style>

		<!--<Style TargetType="Label">
            <Setter Property="TextColor" Value="{StaticResource App_TextColor_LightTheme}" />
        </Style>-->
	</toolkit:Popup.Resources>
	
    <VerticalStackLayout Padding="20" Spacing="10">
		<Label Text="{Binding FullName}" />

		<CollectionView ItemsSource="{Binding MemberPasses}"
						SelectionMode="None">
			<CollectionView.ItemTemplate>
				<DataTemplate>
					<controls:CollectionView_MemberPass BindingContext="{Binding}" />
				</DataTemplate>
			</CollectionView.ItemTemplate>
		</CollectionView>
    </VerticalStackLayout>
    
</toolkit:Popup>

<!--Style="{StaticResource StaticLabelStyle}"-->